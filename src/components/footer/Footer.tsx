import React from 'react';
import "./Footer.css";
import PropTypes from 'prop-types';

Footer.propTypes = {
    
};

function Footer() {
    return (
        <footer>

        </footer>
    );
}

export default Footer;