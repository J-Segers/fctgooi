import React from 'react';
import "./Hero.css";
import PropTypes from 'prop-types';

Hero.propTypes = {
    
};

function Hero() {
    return (
        <div id="hero">
            &nbsp;
        </div>
    );
}

export default Hero;